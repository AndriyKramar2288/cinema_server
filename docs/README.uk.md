# Cinema Server API

Backend для системи онлайн-бронювання квитків у кінотеатр. Повноцінний REST API з повним функціоналом управління сеансами, бронюваннями та інтеграцією з Google Authentication.

[🇺🇸 English](../README.md) | [🇺🇦 Українська](README.uk.md)

## Основні можливості

- **Автентифікація через Google** - безпечний вхід через Google One Tap Sign In
- **Управління фільмами** - повний CRUD для фільмів, сеансів та залів
- **Система бронювання** - онлайн-резервування місць у кінозалах
- **Рольова модель** - підтримка USER, ADMIN та WORKER ролей
- **Selenium парсинг** - можливе отримання інформації про фільми шляхом парсингу даних з UA Kino 
- **REST API** - повноцінний RESTful інтерфейс з валідацією

## Технологічний стек

- **Java 17**
- **Spring Boot 3.4.4**
- **Spring Security** - OAuth2 Resource Server
- **Spring Data JPA / Hibernate** - ORM для роботи з БД
- **PostgreSQL** - реляційна база даних
- **JWT (Auth0)** - токени для авторизації
- **Selenium + JSoup** - парсинг даних з веб-сайтів
- **Docker** - контейнеризація додатку
- **Maven** - система збірки

## Функціональні модулі

### Аутентифікація
- Вхід через Google ID Token
- JWT токени з налаштовуваним терміном дії
- Автоматичне призначення ролей на основі email

### Управління фільмами
- Створення, читання, видалення фільмів
- Парсинг інформації з UA Kino (назва, жанри, актори, постери, тощо)
- Зберігання рейтингів (IMDB)
- Прив'язка фільмів до сеансів

### Система сеансів
- Створення сеансів з прив'язкою до залів
- Автоматична перевірка доступності часу
- Фільтрація доступних сеансів (мін. 20 хв до початку)
- Підрахунок вільних місць

### Бронювання квитків
- Резервування місць для авторизованих та гостьових користувачів
- Валідація унікальності місць
- Перевірка доступності сеансів
- Інформація про глядачів (email, телефон, ПІБ)

### Адміністративна панель
- Повне управління фільмами, сеансами та залами
- Видалення бронювань
- Перегляд всіх сеансів з деталями

## API Endpoints

### Публічні endpoints
```
POST   /auth/google_id_token    - Авторизація через Google
GET    /films/with_sessions     - Фільми з доступними сеансами
GET    /session/{id}            - Сеанси для конкретного фільму
GET    /session/available/by_film/{film_id} - Доступні сеанси
GET    /hall/                   - Список кінозалів
POST   /booking/                - Створення бронювання
```

### Для авторизованих користувачів (USER)
```
GET    /users/                  - Профіль користувача
GET    /users/session/          - Сеанси з бронюваннями користувача
```

### Для адміністраторів (ADMIN)
```
GET    /films/                  - Всі фільми
POST   /films/                  - Створити фільм
DELETE /films/{id}              - Видалити фільм
GET    /films/uakino?request=   - Пошук фільмів на UA Kino
POST   /session/                - Створити сеанс
DELETE /session/{id}            - Видалити сеанс
POST   /hall/                   - Створити залу
DELETE /hall/{id}               - Видалити залу
DELETE /booking/{id}            - Видалити бронювання
```

### Для працівників (WORKER)
```
GET    /session/available_worker - Майбутні сеанси з повною інформацією
```

## Швидкий старт

### 1. Клонування репозиторію
```bash
git clone https://github.com/AndriyKramar2288/cinema_server.git
cd cinema_server
```

### 2. Підготовка бази даних
Створіть PostgreSQL базу даних. Схема буде створена автоматично через Hibernate.

### 3. Конфігурація додатку
Створіть файл `src/main/resources/application.yaml` за зразком, вказаним у файлі `application-example.yaml` у тій ж теці.
Для цього скопіюйте увесь вміст зразка та вкажіть значення для тих змінних, що вказані коментарями.

### 4. Отримання Google Client ID
1. Перейдіть на [Google Cloud Console](https://console.cloud.google.com/)
2. Створіть новий проект або оберіть існуючий
3. Створіть OAuth 2.0 Client ID
4. Скопіюйте Client ID у відповідний параметр в `application.yaml`

### 5. Запуск додатку

#### Через Maven Wrapper
```bash
./mvnw spring-boot:run
```

#### Збірка JAR
```bash
./mvnw clean package -DskipTests
java -jar target/cinema_server-0.0.1-SNAPSHOT.jar
```

Додаток буде доступний за адресою: `http://localhost:8080`

## Deploy

Даний застосунок, окрім як з прямим використанням `Dockerfile` та використанням побудованого образу у відповідних середовищах, передбачає ще й можливість використання Docker Compose для комплексного запуску застосунку.

### Збірка образу
```bash
docker build -t cinema-server:latest .
```

### Збірка та запуск Docker Compose

У разі використання docker compose, виконайте наступні дії:
#### 1. Підготовка проекту

Клонуйте обидва репозиторії в одну директорію:

```bash
# Створіть робочу директорію
mkdir cinema-app
cd cinema-app

# Клонуйте backend
git clone https://github.com/AndriyKramar2288/cinema_server

# Клонуйте frontend
git clone https://github.com/AndriyKramar2288/cinema_client

# Створіть директорію для секретів БД
mkdir db
```

#### 2. Налаштування бази даних

Створіть файл `db/password.txt` з паролем для PostgreSQL:

```bash
echo "your_secure_database_password" > db/password.txt
```

**Важливо**: Переконайтеся, що `db/password.txt` додано до `.gitignore`!

#### 3. Створення конфігурації

Створіть файл `compose.yaml` у кореневій директорії:

```yaml
services:
  server:
    build:
      context: ./cinema_server
    ports:
      - 8080:8080
    secrets:
      - db-password
    depends_on:
      db:
        condition: service_healthy

  client:
    build:
      context: ./cinema_client
    environment:
      - NODE_ENV=production
      - REACT_APP_API_URL=http://localhost:8080
    ports:
      - 3000:3000
    depends_on:
      - server

  db:
    image: postgres:16-alpine
    restart: always
    user: postgres
    secrets:
      - db-password
    volumes:
      - cinema_volume_prod:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=cinema_db
      - POSTGRES_PASSWORD_FILE=/run/secrets/db-password
    expose:
      - 5432
    healthcheck:
      test: ["CMD", "pg_isready"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  cinema_volume_prod:

secrets:
  db-password:
    file: db/password.txt
```

#### 4. Запуск

```bash
# Збірка та запуск усіх сервісів
docker compose up -d --build

# Перегляд логів
docker compose logs -f

# Перегляд логів конкретного сервісу
docker compose logs -f server
docker compose logs -f client
docker compose logs -f db
```

#### 5. Перевірка

Після запуску перевірте доступність сервісів:
- Frontend: http://localhost:3000
- Backend API: http://localhost:8080

#### 6. Управління

```bash
# Зупинка сервісів
docker compose down

# Зупинка з видаленням volumes (УВАГА: видалить БД!)
docker compose down -v

# Перезапуск конкретного сервісу
docker compose restart server

# Перегляд статусу
docker compose ps

# Підключення до БД
docker compose exec db psql -U postgres -d cinema_db
```


## Архітектура додатку

```
src/main/java/com/banew/cinema_server/
├── backend/
│   ├── configs/           # Security, CORS, Jackson
│   ├── controllers/       # REST контролери
│   ├── dto/              # Data Transfer Objects
│   ├── entities/         # JPA сутності
│   ├── repositories/     # Spring Data репозиторії
│   ├── services/         # Бізнес-логіка
│   └── exceptions/       # Кастомні винятки
```

### Основні сутності
- **CinemaUser** - користувачі системи
- **Film** - фільми з метаданими
- **ViewSession** - кіносеанси
- **Hall** - кінозали
- **Booking** - бронювання
- **CinemaViewer** - інформація про глядачів

## Безпека

- JWT токени з підписом HMAC256
- OAuth2 Resource Server
- Валідація Google ID токенів
- Рольовий доступ (ROLE_USER, ROLE_ADMIN, ROLE_WORKER)
- CORS налаштування
- Валідація вхідних даних

## Парсинг фільмів

Система використовує Selenium WebDriver для динамічного парсингу UA Kino:
- Автоматичний пошук фільмів
- Отримання детальної інформації (актори, режисер, жанри)
- Завантаження постерів та кадрів
- Парсинг рейтингів IMDB

**Увага**: У Docker образі встановлено Google Chrome для Selenium.